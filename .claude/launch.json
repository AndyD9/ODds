{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "dashboard",
      "runtimeExecutable": "sh",
      "runtimeArgs": ["-c",
                      "uv run streamlit run src/odds/app/dashboard.py --server.port ${PORT:-8501} --server.headless true"],
      "port": 8501,
      "autoPort": true
    }
  ]
}
